<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kuroi.chance">

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 相机使用 -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application>
        <activity
            android:name=".activity.ChanceMainActivity"
            android:label="@string/app_name_Chance"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.ChanceAddActivity"
            android:label="@string/title_activity_add_Chance"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.ChanceDetailActivity"
            android:label="@string/title_activity_detail_Chance"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ChanceModifyActivity"
            android:label="@string/title_activity_modify_Chance"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />
        <!--<activity-->
        <!--android:name=".activity.ChanceShowActivity"-->
        <!--android:label="@string/title_activity_show_Chance" />-->
        <activity
            android:name=".activity.ChanceKLActivity"
            android:label="@string/title_activity_chance_kl" />
        <activity
            android:name=".activity.ChanceCGActivity"
            android:label="@string/title_activity_chance_cg" />
    </application>

</manifest>
